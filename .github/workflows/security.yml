name: Security checks

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 3 * * *'

jobs:
  pip-audit:
    name: Python dependency audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pip-audit
      - name: Run pip-audit
        run: |
          pip-audit -r requirements.txt --strict || echo "pip-audit found issues" > pip-audit.txt

  trivy-image:
    name: Trivy scan (image)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: lcm:ci-scan
          build-args: |
            TORCH_INDEX_URL=https://download.pytorch.org/whl/cpu
      - name: Trivy scan image (local docker)
        id: trivy
        uses: aquasecurity/trivy-action@0.32.0
        with:
          image-ref: lcm:ci-scan
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          format: sarif
          output: trivy-results.sarif
      - name: Ensure SARIF exists
        run: |
          test -f trivy-results.sarif || echo '{"runs":[{"results":[]}],"version":"2.1.0"}' > trivy-results.sarif
      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-results.sarif

